
@{
    ViewBag.Title = "Index";
}



@if (ViewBag.displayMenu == "Admin")
{

   <div>
        <h1>Welcome Admin. Now you can Manage user Roles.</h1>
    <h3>
        @Html.ActionLink("Manage Roles", "Index", "Role")
    </h3>
   </div>
    
}
else if(ViewBag.displayMenu == "Coach")
{
    <h2>  Welcome <strong>Coach @ViewBag.Name</strong>.We will add a Coach's module soon </h2>

    @Html.ActionLink("Edit Teams", "Index", "Teams");
    @Html.ActionLink("Edit Schedule", "Index", "Schedules");
}
else if (ViewBag.displayMenu == "Player")
{
    <h2>  Welcome <strong>@ViewBag.Name</strong> :) .We will add a Player's module soon </h2>
}

