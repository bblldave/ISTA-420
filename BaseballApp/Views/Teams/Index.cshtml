@model IEnumerable<BaseballApp.Models.Team>

@{
    ViewBag.Title = "Index";
}

<h2>Our Teams</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<div class="upcomingGames">
    <table class="table table-striped">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TeamName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th>

            </th>
            <th>

            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TeamName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.City)
                </td>
                <td>
                    @if (User.IsInRole("Coach"))
                    {
                        @Html.ActionLink("Edit", "Edit", new { id = item.Team1 })
                    }
                </td>
                <td>
                   
                    
                    @Html.ActionLink("Details", "Details", new { id = item.Team1 })
                    @*@Html.ActionLink("Delete", "Delete", new { id=item.Team1 })*@
                </td>
            </tr>
        }

    </table>

</div>




