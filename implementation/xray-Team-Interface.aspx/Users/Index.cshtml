
@{
    ViewBag.Title = "Index";
}



@if (ViewBag.displayMenu == "Admin")
{

   <div>
        <h1>Welcome Admin. Now you can Manage user Roles.</h1>
    <h3>
        @Html.ActionLink("Manage Role", "Index", "Role")
    </h3>
   </div>
    
}
else if(ViewBag.displayMent == "Coach")
{
    <h2>  Welcome <strong>@ViewBag.Name</strong> :) .We will add a Coach's module soon </h2>
}
else if (ViewBag.displayMent == "Player")
{
    <h2>  Welcome <strong>@ViewBag.Name</strong> :) .We will add a Player's module soon </h2>
}

